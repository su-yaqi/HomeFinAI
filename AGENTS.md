# HomeFin Repository Instructions

本文件适用于整个仓库。开始工作前先阅读本文件、相关目录文档和 `context/` 当前事实。

## 安全边界

- 优先通过正常、可验证的方式解决问题。正常途径无法解决、连续尝试仍失败，或下一步
  需要降级、绕过检查、高风险操作时，立即停止并说明问题，未经确认不得继续。
- 保护现有工作区改动。不得擅自 stash、覆盖、搬运、丢弃或假设改动已经提交。
- 不使用 `git reset --hard`、强推或历史改写；不擅自删除文件、分支或工作区。
- 未经用户或维护者明确授权，不推送远端、不合并默认分支、不发布 Release、不部署，
  也不修改正式服务器或正式数据。
- 内网正式部署保持 HTTP、固定访问地址和受控持久化边界；开发自动化不得改变其语义。

## 开始工作

1. 核实当前分支、工作区状态、相关 `context/` 和受影响模块。
2. 当前分支目标与任务一致时继续；否则创建 `feat/`、`fix/` 或 `chore/` 短分支。
   Codex 创建的分支使用 `codex/` 前缀。
3. 在修改前声明变更范围、风险等级和预期验证，不把无关问题顺带纳入。
4. 使用 `make change-plan` 查看基于路径推断的最低风险；路径判断只是下限，语义风险
   更高时必须主动提高等级。

## S/M/H 风险等级

### S — 低风险微调

- 适用：文案、文档、样式、测试或局部实现微调，不改变外部契约、持久化结构或权限。
- 要求：格式/静态检查，以及与改动直接相关的最小测试。
- 不得以 S 规避本应执行的受影响测试。

### M — 普通功能变更

- 适用：普通前后端功能、非安全 API 行为、局部工程流程或单模块重构。
- 要求：执行受影响模块的测试；记录 `context impact`。只有已实现语义事实变化时更新
  对应 `context`，无语义影响时记录原因。

### H — 高风险变更

- 适用：认证、权限、安全边界、数据库模型与迁移、依赖或锁文件、部署、正式配置、
  跨模块架构。
- 要求：完整相关测试；数据库改动需升级/降级或迁移演练；提供回滚方案与验证结果；
  在进入正式发布前完成 staging 验证。
- 只有不涉及数据库迁移时，迁移证据可以写明 N/A 原因；回滚和 staging 不允许用
  N/A 代替。缺少 staging 或权限时，H 级变更保持未完成并报告阻塞。
- 迁移失败时停止并报告；不得把 schema downgrade 当作数据自动回滚。

## Context 规则

- 每个变更都要记录 `context impact`：`updated` 或 `none`。
- `updated`：代码改变了当前已实现的产品语义、接口、数据、架构或运行边界，更新对应
  根级文件、`modules/*` 和实际交付 changelog。
- `none`：没有改变上述事实，在 PR/交付记录中写明原因；不要为了满足形式机械修改
  `context`。
- 设计设想写入 PRD 或设计文档；只有实际交付内容才能写成当前事实。

## 完成定义

完成必须同时满足：

1. 范围已收敛，没有混入无关改动。
2. 已执行与风险匹配的验证，并记录命令和结果；失败不得描述为通过。
3. 已处理并记录 `context impact`。
4. 改动按单一可说明目标形成逻辑提交。
5. 分支、提交、测试、剩余风险和后续动作可追溯。

使用 `make change-check` 验证声明和证据。H 级还必须提供回滚、迁移和 staging 证据。

## 分支、提交与版本

- 默认分支必须始终保持可发布；不直接把未验证工作提交到默认分支。
- 每个提交只覆盖一个可说明目标。提交前运行与风险匹配的验证。
- 在已获准推送且远端可用时，于首个稳定提交和工作结束时推送；若当前任务禁止推送，
  保留本地提交并明确报告。
- 合并后只删除已确认合并且已获授权删除的短分支。
- 版本通过 tag 和 GitHub Release 表达，不新增长期版本开发分支。
