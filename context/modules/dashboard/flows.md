# dashboard 业务流程

## 核心流程
1. 用户进入 `/`。
2. 页面请求 `/dashboard/`。
3. 后端按 `BUSINESS_TIMEZONE` 确定当前业务日期，再按当前用户聚合本月交易、最近 6 个月交易和当年预算使用情况。
4. 页面以卡片、列表和进度条形式展示聚合结果。

## 业务规则
- Dashboard 只聚合当前用户数据。
- 预算使用金额只统计支出类交易。
- Dashboard 不提前统计业务日期之后的未来交易。
