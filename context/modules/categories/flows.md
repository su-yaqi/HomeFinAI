# categories 业务流程

## 核心流程
1. 用户进入 `/system/categories`。
2. 页面按统一分页规则读取当前用户分类列表。
3. 用户可创建分类，并可选择父分类。
4. 用户可通过颜色选择器选色，也可直接输入十六进制色号。
5. 用户可修改名称、颜色和父分类；后端会沿父链检查，拒绝形成任意层级环。
6. 删除时，后端会检查是否存在子分类或关联交易。

## 业务规则
- 分类是交易和 Dashboard 的基础字典数据。
- 当前前端路径带有 `system` 前缀，但该页面并非管理员独占。
